auth:
  rootPassword: "rootPassword"
  username: admin
  password: adminPassword
  database: eletricity_usage

architecture: standalone

replicaSet:
  enabled: false

service:
  type: ClusterIP
  port: 27017
